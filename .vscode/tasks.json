{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "switch home",
      "type": "shell",
      "command": "just switch-home",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    },
    {
      "label": "switch system",
      "type": "shell",
      "command": "just switch-system",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
